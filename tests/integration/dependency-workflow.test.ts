import { describe,expect,it } from 'vitest';import { testService } from '../helpers/service.js';
describe('dependency workflow',()=>{it('will not analyze a task whose mandatory dependency is not READY',async()=>{const {service}=testService();const p=await service.projectCreate({name:'Dependencies',slug:'dependencies',sourceType:'LOCAL',environment:'SANDBOX',autonomyMode:'OBSERVE',workspacePath:'x'});const first=await service.taskCreate({projectId:p.id,externalKey:'D-1',title:'First',description:'first',requirements:['first'],relationships:[]});const second=await service.taskCreate({projectId:p.id,externalKey:'D-2',title:'Second',description:'second',requirements:['second'],relationships:[{type:'DEPENDS_ON',targetTaskId:first.id}]});await expect(service.taskAnalyze(p.id,second.id)).rejects.toMatchObject({code:'DEPENDENCY_BLOCKED'});expect((await service.taskGet(p.id,second.id)).state).toBe('INGESTED');});});
