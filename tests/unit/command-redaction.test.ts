import { describe,expect,it } from 'vitest';import { CommandRunner } from '../../packages/execution-engine/src/command-runner.js';import { CommandPolicy } from '../../packages/execution-engine/src/command-policy.js';import { systemClock } from '../../packages/core/src/ports.js';
describe('command argument redaction',()=>{it('executes sensitive arguments but never journals their value',async()=>{const runner=new CommandRunner(new CommandPolicy(),systemClock);const taskId=crypto.randomUUID();await runner.run({command:'node',args:['--version','temporary-password-value'],cwd:process.cwd(),taskId,allowed:['TEST'],sensitiveArgIndexes:[1]});const entry=runner.drain(taskId)[0];expect(entry?.record.command).toContain('[REDACTED]');expect(JSON.stringify(entry?.record)).not.toContain('temporary-password-value');});});
