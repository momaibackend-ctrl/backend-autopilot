import { describe,expect,it } from 'vitest';import { ExternalPostgresAdapter } from '../../packages/adapters/database/src/index.js';
describe('safe migration layer',()=>{it('rejects destructive database statements before resolving credentials',async()=>{const adapter=new ExternalPostgresAdapter({get:async()=>{throw new Error('must not resolve');}});await expect(adapter.applyMigrations({resourceId:crypto.randomUUID(),type:'DATABASE',provider:'postgres',externalReference:'sandbox',projectId:crypto.randomUUID(),environment:'SANDBOX',permissions:['MIGRATE'],status:'ACTIVE',secretRefs:['SANDBOX_DATABASE_URL'],createdAt:new Date().toISOString()},[{name:'bad_migration',sql:'DROP DATABASE production',rollback:'restore backup'}])).rejects.toMatchObject({code:'ARCHITECTURE_VIOLATION'});});});
