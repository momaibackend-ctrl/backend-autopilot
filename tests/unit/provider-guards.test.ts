import { describe,expect,it } from 'vitest';import { LiveGitHubAdapter } from '../../packages/adapters/github/src/index.js';import { LiveSupabaseCliAdapter } from '../../packages/adapters/supabase/src/index.js';import type { CommandRunner } from '../../packages/execution-engine/src/command-runner.js';
const projectId=crypto.randomUUID();describe('live provider sandbox guards',()=>{it('rejects non-sandbox GitHub repository names before any command',async()=>{const adapter=new LiveGitHubAdapter({} as CommandRunner);await expect(adapter.createSandboxRepository({resourceId:crypto.randomUUID(),type:'GITHUB_ACCOUNT',provider:'github',externalReference:'sandbox-user',projectId,environment:'SANDBOX',permissions:['ADMIN'],status:'ACTIVE',secretRefs:[],createdAt:new Date().toISOString()},{workspace:process.cwd(),name:'production-api',description:'x',correlationId:projectId})).rejects.toMatchObject({code:'POLICY_VIOLATION'});});it('rejects non-sandbox Supabase project names before creating credentials',async()=>{const adapter=new LiveSupabaseCliAdapter({} as CommandRunner,{get:async()=>'',set:async()=>{},revoke:async()=>{}});await expect(adapter.createSandboxProject({resourceId:crypto.randomUUID(),type:'SUPABASE_ORGANIZATION',provider:'supabase',externalReference:'org',projectId,environment:'SANDBOX',permissions:['ADMIN'],status:'ACTIVE',secretRefs:[],createdAt:new Date().toISOString()},{cwd:process.cwd(),name:'production-db',region:'us-east-1',secretPrefix:'AUTOPILOT_TEST',correlationId:projectId})).rejects.toMatchObject({code:'POLICY_VIOLATION'});});});
