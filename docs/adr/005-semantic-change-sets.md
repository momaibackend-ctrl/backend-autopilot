# ADR 005: Semantic file change sets

Status: accepted. MCP submits validated file changes and operation IDs, not commands. Execution validates workspace containment, uses Git evidence and delegates only fixed test commands through CommandPolicy.
