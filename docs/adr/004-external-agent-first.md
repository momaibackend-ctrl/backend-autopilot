# ADR 004: External-agent-first runtime

Status: accepted. MCP is the first execution mode. `AgentRuntime` separates external and future internal providers; no direct model SDK calls are permitted outside an LLM adapter.
