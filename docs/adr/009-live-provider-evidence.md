# ADR 009: Live provider evidence and semantic destruction

Status: accepted. Capability status is derived from current probes and successful audit evidence. Interfaces are never reported as live. GitHub and Supabase creation use official CLI/API adapters. Deletion has separate semantic tools with resource-bound confirmation; it is never hidden in a generic command.
